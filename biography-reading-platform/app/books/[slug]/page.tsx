import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {SiteHeader} from "@/components/SiteHeader";import {SiteFooter} from "@/components/SiteFooter";import {getBook,getBooks} from "@/lib/content";
export function generateStaticParams(){return getBooks().map(b=>({slug:b.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const b=getBook((await params).slug);return b?{title:b.title,description:b.summary}:{}}
export default async function BookPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const b=getBook(slug);if(!b)notFound();return <><SiteHeader/><main className="page-shell"><div className="breadcrumbs"><Link href="/">首页</Link>　/　<Link href="/books">书架</Link>　/　{b.title}</div><section className="profile-hero"><div className="book-card"><div className="cover"><small>人物志 · 精选</small><b>{b.title}</b><small>{b.author}</small></div></div><div className="profile-title"><p className="eyebrow">BOOK PROFILE</p><h1 style={{fontSize:46}}>{b.title}</h1><p className="alias">{b.author}</p><p className="lead">{b.summary}</p><div className="facts"><div><span>出版社</span><b>{b.publisher}</b></div><div><span>出版时间</span><b>{b.publishDate}</b></div><div><span>ISBN</span><b>{b.isbn}</b></div></div><Link className="buy-button" href={`/go/${b.slug}`}>立即购买 →</Link></div></section><article className="prose" style={{maxWidth:760,margin:"55px auto"}} dangerouslySetInnerHTML={{__html:b.content}}/></main><SiteFooter/></>}

